#include <iostream>
#include <vector>

using namespace std;

int main() {

    vector<int> v{1, 2, 3, 4, 5};

    // 1. use standard method to iterate through the vector

    // 2. use iterator to iterate through the vector

    // 3. use auto to make your life easier

    // 4. use elem : vector to make it even easier

    // 5. use v.cbegin() and try to modify the element

}