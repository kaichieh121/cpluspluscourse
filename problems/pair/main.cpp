#include <iostream>
#include <vector>

using namespace std;

int main() {

    // make a pair of string and int
    // print out the member variables
    // make another pair using makepair()
    // swap the two pairs

    // do the same for tuple

    // Let's practice set stl
    // make a set
    // Add numbers (duplicates and unique)
    // Remove a number
    // Check if a number exists
    // Display all numbers

}