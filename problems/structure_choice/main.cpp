#include <iostream>
#include <vector>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>
#include <array>
#include <algorithm>

using namespace std;

// Problem 1: Count Unique Elements
int countUniqueElements(const vector<int>& nums) {
}

// Problem 2: Find Most Frequent Element
// int findMostFrequentElement(const vector<int>& nums) {
// }

// Problem 3: Smallest Missing Positive Integer
// int findSmallestMissingPositive(const vector<int>& nums) {

// }

// Problem 4: Leaderboard Ranking
// class Leaderboard {
// private:

// public:

// };

int main() {
    // Problem 1: Count Unique Elements
    cout << "Problem 1: Count Unique Elements\n";
    vector<int> nums1 = {1, 2, 2, 3, 4, 4, 5};
    cout << "Unique elements count: " << countUniqueElements(nums1) << "\n";

    // Problem 2: Find Most Frequent Element
    // cout << "\nProblem 2: Find Most Frequent Element\n";
    // vector<int> nums2 = {1, 3, 2, 3, 1, 3, 2, 1, 1, 1};
    // cout << "Most frequent element: " << findMostFrequentElement(nums2) << "\n";

    // Problem 3: Smallest Missing Positive Integer
    // cout << "\nProblem 3: Smallest Missing Positive Integer\n";
    // vector<int> nums3 = {3, 4, -1, 1};
    // cout << "Smallest missing positive integer: " << findSmallestMissingPositive(nums3) << "\n";

    // Problem 4: Leaderboard Ranking
    // cout << "\nProblem 4: Leaderboard Ranking\n";
    // Leaderboard leaderboard;
    // leaderboard.updateScore(1, 50);
    // leaderboard.updateScore(2, 100);
    // leaderboard.updateScore(3, 75);
    // cout << "Player with highest score: Player " << leaderboard.getTopPlayer() << "\n";

    return 0;
}
