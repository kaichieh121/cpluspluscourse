#include "vector.hpp"
#include <iostream>
#include <string>
#include <array>
#include <stack>
#include <queue>
#include <vector>

using namespace std;


int main() {

    // create an array of integer, size 5
    // fill the elements
    // print out every element


    // create a vector of integer
    // place 1, 2, 3, 4, 5 in the vector
    // double every element
    // print out the element


    // Push elements onto the stack
    // Pop an element from the stack
    // Peek the top element
    std::stack<int> st;

    
    // Enqueue elements into the queue
    // Dequeue an element from the queue
    // Peek the front element
    std::queue<int> q;


    return 0;


}